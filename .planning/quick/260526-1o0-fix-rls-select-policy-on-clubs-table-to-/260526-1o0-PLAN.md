---
quick_id: 260526-1o0
slug: fix-rls-select-policy-on-clubs-table-to-
description: Fix RLS SELECT policy on clubs table to allow creator to read their own club
date: 2026-05-26
status: planned
must_haves:
  truths:
    - clubs_select_public_or_member policy includes created_by = auth.uid() condition
    - Private club INSERT + SELECT chain succeeds for the creator
  artifacts:
    - supabase/migrations/ new migration file adding the updated policy
---

# Quick Plan: Fix RLS SELECT policy on clubs table

## Task 1 — Add `created_by = auth.uid()` to clubs SELECT policy

**files:** `supabase/migrations/`
**action:** Apply a new Supabase migration that drops and recreates the `clubs_select_public_or_member` policy with a third condition: `created_by = auth.uid()`. This allows the club creator to read their own club immediately after INSERT, before they are inserted as a member.
**verify:** Policy definition in pg_policies shows three conditions: is_public, EXISTS(member), created_by = auth.uid()
**done:** Migration applied; test INSERT+SELECT for a private club succeeds
